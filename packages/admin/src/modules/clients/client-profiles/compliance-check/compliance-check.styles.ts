import {
	css,
} from '@emotion/css'
import {
	spaces,
} from '../../../../shared/styles'

export const pageWrapper = css`
	padding-right: ${spaces.medium};
	display: flex;
	flex-direction: column;
	gap: ${spaces.medium};
`